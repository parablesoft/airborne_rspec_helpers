require 'spec_helper'

describe AirborneRspecHelpers do
  it 'has a version number' do
    expect(AirborneRspecHelpers::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
